event
{
 MakeWall(1);
 MakeFish(2);
 HookRetrace(1);
 playerx=10; playery=13;
 fisherx=10; fishery=8;
 SharkLoop();
 FadeOut(100);
 stage=2;
 if (game=1) map("inter.map");
 else map("intro.map");
}

event
{
 Wavey();
 FishRender();
}
