event
{
 MakeWall(1);
 MakeFish(2);
 HookRetrace(1);
 playerx=12; playery=13;
 fisherx=12; fishery=10;
 SharkLoop();
 FadeOut(100);
 stage=3;
 if (game=1) map("inter.map");
 else map("intro.map");
}

event
{
 Wavey();
 FishRender();
}
