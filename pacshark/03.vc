event
{
 MakeWall(1);
 MakeFish(2);
 HookRetrace(1);
 playerx=15; playery=11;
 fisherx=15; fishery=8;
 SharkLoop();
 FadeOut(100);
 stage=4;
 if (game=1) map("inter.map");
 else map("intro.map");
}

event
{
 Wavey();
 FishRender();
}
